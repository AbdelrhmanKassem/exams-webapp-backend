{
  "type": "object",
  "required": ["branches", "meta"],
  "properties": {
    "branches": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "name", "student_count", "exam_count"],
        "properties": {
          "id": {"type":"integer"},
          "name": {"type":"string"},
          "student_count": {"type":"integer"},
          "exam_count": {"type":"integer"}
        }
      }
    },
    "meta":  {"$ref": "page_meta_data.json"}
  }
}