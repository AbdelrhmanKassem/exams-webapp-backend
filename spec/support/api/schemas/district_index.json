{
  "type": "object",
  "required": ["districts", "meta"],
  "properties": {
    "districts": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "name", "governorate", "school_count"],
        "properties": {
          "id": {"type":"integer"},
          "name": {"type":"string"},
          "governorate": {"type":"string"},
          "school_count": {"type":"integer"}
        }
      }
    },
    "meta":  {"$ref": "page_meta_data.json"}
  }
}