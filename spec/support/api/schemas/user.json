{
  "required": ["id", "email", "first_name", "last_name", "role"],
  "type": "object",
  "properties": {
      "id": {"type":"integer"},
      "email": {"type":"string"},
      "first_name": {"type":"string"},
      "last_name": {"type": "string"},
      "role": {"$ref": "role.json"}
  }
}
