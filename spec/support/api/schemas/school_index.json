{
  "type": "object",
  "required": ["schools", "meta"],
  "properties": {
    "schools": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "name"],
        "properties": {
          "id": {"type":"integer"},
          "name": {"type":"string"}
        }
      }
    },
    "meta":  {"$ref": "page_meta_data.json"}
  }
}