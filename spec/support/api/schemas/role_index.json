{
  "type": "object",
  "required": ["roles", "meta"],
  "properties": {
    "roles": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "name", "user_count"],
        "properties": {
          "id": {"type":"integer"},
          "name": {"type":"string"},
          "user_count": {"type":"integer"}
        }
      }
    },
    "meta":  {"$ref": "page_meta_data.json"}
  }
}